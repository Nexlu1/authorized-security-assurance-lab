# Temporary hold-release PR bootstrap

This file is non-authoritative and exists only to create the protected pull request so its real GitHub URL and creation timestamp can be bound into the canonical control transition.

It grants no authorization, changes no gate, releases no hold, and will be removed before merge.
